apiVersion: v1
kind: Service
metadata:
  name: {{ include "hackthecrous.fullname" . }}
  labels:
    {{- include "hackthecrous.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hackthecrous.selectorLabels" . | nindent 4 }}
